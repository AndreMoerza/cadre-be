import { Injectable } from '@nestjs/common';
/**
 * Product Jobs Provider
 * this class is used to provide jobs for product module, you can utilize this class
 * to create jobs for product module e.g. sending email, sending sms, cron job, scheduling, etc.
 * @export
 * @class ProductJobsProvider
 */
@Injectable()
export class ProductJobsProvider {}
