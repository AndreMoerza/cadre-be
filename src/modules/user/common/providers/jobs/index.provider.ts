import { Injectable } from '@nestjs/common';
/**
 * User Jobs Provider
 * this class is used to provide jobs for user module, you can utilize this class
 * to create jobs for user module e.g. sending email, sending sms, cron job, scheduling, etc.
 * @export
 * @class UserJobsProvider
 */
@Injectable()
export class UserJobsProvider {}
