import { Injectable } from '@nestjs/common';
/**
 * Sale Jobs Provider
 * this class is used to provide jobs for sale module, you can utilize this class
 * to create jobs for sale module e.g. sending email, sending sms, cron job, scheduling, etc.
 * @export
 * @class SaleJobsProvider
 */
@Injectable()
export class SaleJobsProvider {}
