import { Injectable } from '@nestjs/common';

/**
 * Sale Cache Provider
 * this class is used to provide cache for sale module, you can utilize this class
 * to create cache for sale module e.g. caching sale data, caching sale profile, etc.
 * @export
 * @class SaleCacheProvider
 */
@Injectable()
export class SaleCacheProvider {}
