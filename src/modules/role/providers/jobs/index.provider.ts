import { Injectable } from '@nestjs/common';
/**
 * Role Jobs Provider
 * this class is used to provide jobs for role module, you can utilize this class
 * to create jobs for role module e.g. sending email, sending sms, cron job, scheduling, etc.
 * @export
 * @class RoleJobsProvider
 */
@Injectable()
export class RoleJobsProvider {}
