import { Injectable } from '@nestjs/common';

/**
 * Role Cache Provider
 * this class is used to provide cache for role module, you can utilize this class
 * to create cache for role module e.g. caching role data, caching role profile, etc.
 * @export
 * @class RoleCacheProvider
 */
@Injectable()
export class RoleCacheProvider {}
