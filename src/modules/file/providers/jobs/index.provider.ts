import { Injectable } from '@nestjs/common';
/**
 * File Jobs Provider
 * this class is used to provide jobs for file module, you can utilize this class
 * to create jobs for file module e.g. sending email, sending sms, cron job, scheduling, etc.
 * @export
 * @class FileJobsProvider
 */
@Injectable()
export class FileJobsProvider {}
