import { Injectable } from '@nestjs/common';

/**
 * Auth Cache Provider
 * this class is used to provide cache for auth module, you can utilize this class
 * to create cache for auth module e.g. caching auth data, caching auth profile, etc.
 * @export
 * @class AuthCacheProvider
 */
@Injectable()
export class AuthCacheProvider {}
