import { Injectable } from '@nestjs/common';
/**
 * Employee Jobs Provider
 * this class is used to provide jobs for employee module, you can utilize this class
 * to create jobs for employee module e.g. sending email, sending sms, cron job, scheduling, etc.
 * @export
 * @class EmployeeJobsProvider
 */
@Injectable()
export class EmployeeJobsProvider {}
