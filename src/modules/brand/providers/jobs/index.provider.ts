import { Injectable } from '@nestjs/common';
/**
 * Brand Jobs Provider
 * this class is used to provide jobs for brand module, you can utilize this class
 * to create jobs for brand module e.g. sending email, sending sms, cron job, scheduling, etc.
 * @export
 * @class BrandJobsProvider
 */
@Injectable()
export class BrandJobsProvider {}
