import { Injectable } from '@nestjs/common';
/**
 * ProductType Jobs Provider
 * this class is used to provide jobs for productType module, you can utilize this class
 * to create jobs for productType module e.g. sending email, sending sms, cron job, scheduling, etc.
 * @export
 * @class ProductTypeJobsProvider
 */
@Injectable()
export class ProductTypeJobsProvider {}
