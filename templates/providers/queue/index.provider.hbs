import { Injectable } from '@nestjs/common';

/**
 * {{pascalModName}} Queue Provider
 * this class is used to provide queue for {{lowerModName}} module, you can utilize this class
 * to create queue for {{lowerModName}} module e.g. sending email, sending sms, cron job queue, scheduling queue, etc.
 * @export
 * @class {{pascalModName}}QueueProvider
 */
@Injectable()
export class {{pascalModName}}QueueProvider {}
